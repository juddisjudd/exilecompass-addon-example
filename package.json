{
  "name": "@exilecompass/notes-addon-example",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "esbuild src/panel.ts --bundle --format=esm --platform=browser --outfile=dist/panel.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.2"
  }
}
